Update STM_main.py to use the stm_core functions
Implement the proper handling of projection when the shrinkwrap has been artificially closed. Enforce 0.0 to non-touching surfaces. Missing in STM_main.py
make another document with standard post-processing snippets, and just call objects from STM_result_handler
